#ifdef ARDUINO
#include <Arduino.h>
#endif

#ifndef Animation_h
#define Animation_h

#include "Vector.h"
#include "Graphics.h"

class Animation {

    public:
        static void runPlaneAnim() {
            // draw first plane 
            // Graphics.drawLine(Point3D(0,0,0), Vector3D., Coloring, millis);

            // move the plane via y axxis

        };

};

#endif
